# متطلبات المرحلة الثانية — منصة ترتيلة Online

> **الغرض من الملف:** المرجع الموحد لجميع التعديلات والإضافات المطلوبة بعد التسليم الأول للمنصة.  
> أي طلب جديد يتم اعتماده لاحقًا يجب إضافته إلى هذا الملف بدل إنشاء قوائم منفصلة أو الاعتماد على رسائل متفرقة.

## 1. قواعد إدارة هذا الملف

- هذا الملف يوثق **نطاق العمل المطلوب** فقط، ولا يعني أن البنود نُفذت بالفعل.
- الحالات المستخدمة:
  - `مطلوب`: تم طلب الميزة ولم يبدأ تنفيذها.
  - `قيد التحليل`: تحتاج قرارًا أو توضيحًا تجاريًا قبل التنفيذ.
  - `قيد التنفيذ`: بدأ العمل عليها.
  - `مكتمل`: تم تنفيذها واختبارها واعتمادها.
  - `مؤجل`: خرجت من الإصدار الحالي.
- أي تغيير في قاعدة عمل معتمدة يُسجل في **سجل التغييرات** أسفل الملف.
- تطبيق الموبايل نطاق مستقل، وليس جزءًا من هذه المرحلة إلا بعد اعتماد عرض منفصل.

---

## 2. ملخص النطاق والأولوية

| # | الميزة | النوع | الأولوية | الحالة |
|---|---|---|---|---|
| 1 | إنشاء المعلم مع طلابه من مكان واحد | تطوير وربط | حرجة للتشغيل | مكتمل |
| 2 | تصنيفات المعلمين والطلاب | تطوير | عالية | مكتمل |
| 3 | دمج وتطوير الجداول | تطوير وربط | حرجة للتشغيل | مكتمل |
| 4 | حساب المواعيد الشاغرة للمعلم تلقائيًا | إضافة جديدة | حرجة للتشغيل | مكتمل |
| 5 | تسجيل اشتراك ورصيد الطالب الحالي | تطوير | حرجة للتشغيل | مكتمل |
| 6 | حساب راتب المعلم بالساعة | تطوير | حرجة للتشغيل المالي | قيد التنفيذ |
| 7 | المكافآت والخصومات والتسويات | تطوير وإضافة | عالية | مطلوب |
| 8 | ملف إداري متكامل للمعلم | تطوير | عالية | قيد التنفيذ |
| 9 | تجديد الاشتراك | تطوير | متوسطة | مطلوب |
| 10 | تقرير الحلقة القرآنية | تطوير | عالية | مطلوب |
| 11 | استبيان التقييم والتجديد | إضافة جديدة | متوسطة | مطلوب |
| 12 | متابعة التقارير والحصص الشهرية | تطوير وإضافة | عالية | مطلوب |
| 13 | التقرير الشهري للمعلم | إضافة جديدة مبنية على بيانات حالية | متوسطة | مطلوب |
| 14 | طلب إسناد طالب جديد وقبول/رفض المعلم | إضافة جديدة جزئيًا | حرجة للتشغيل | مكتمل |
| 15 | الإشعارات والصلاحيات وسجل العمليات | تطوير مشترك | ممتدة على كل البنود | قيد التنفيذ |

---

## 3. نطاق الإصدار التشغيلي الأول

هذه البنود تُنفذ أولًا حتى تستطيع الأكاديمية إضافة المعلمين والطلاب وتشغيل الحصص بصورة صحيحة أثناء استكمال باقي المرحلة:

1. إنشاء الطالب من لوحة الإدارة.
2. إنشاء المعلم وإضافة طلابه من نفس التدفق.
3. اختيار الباقة وتسجيل الرصيد الافتتاحي.
4. إعداد أوقات عمل المعلم وإظهار المواعيد الشاغرة تلقائيًا.
5. إنشاء جدول الطالب وربطه بالمعلم والاشتراك.
6. تحديد الطالب كـ`قديم` أو `جديد` وتطبيق مسار الإسناد المناسب.
7. حساب راتب المعلم بالساعة.
8. ملف المعلم الإداري الأساسي والصلاحيات اللازمة.

---

## 4. إنشاء المعلم مع طلابه من مكان واحد

**الحالة:** مكتمل — إنشاء الحسابات والتصنيفات وأوقات العمل والباقة والرصيد الافتتاحي (الجزء الأول) + خيارات بيانات الدخول واختيار الموعد الشاغر وإنشاء الجدول الدوري والإسناد (الجزء الثاني) — منفذ ومختبر يدويًا في متصفح حي (انظر "متابعة استكمال الجزء الأول وتنفيذ الجداول والإسناد")  
**النوع:** تطوير وربط أنظمة موجودة

### المطلوب

- إنشاء حساب المعلم وبيانات الدخول.
- تحديد تخصص المعلم والفئات المناسبة له.
- تحديد سعر ساعة التدريس.
- تحديد أوقات العمل المتاحة لكل يوم.
- إضافة طالب واحد أو عدة طلاب أثناء إنشاء المعلم.
- إنشاء حساب مستقل لكل طالب.
- تحديد نوع الطالب: `قديم` أو `جديد`.
- اختيار باقة الطالب وتسجيل رصيده الحالي.
- ربط الطالب بالمعلم.
- اختيار موعد شاغر وإنشاء الجدول الدوري.
- إضافة طلاب جدد لاحقًا من ملف المعلم.
- تعديل بيانات الطالب أو نقله إلى معلم آخر حسب الصلاحية.

### خارج النطاق

- إدخال بيانات الطلاب الحاليين يدويًا بواسطة المطور.
- استيراد Excel جماعي، ما لم يُعتمد لاحقًا كبند مستقل.

### شروط القبول

- لا يمكن إنشاء مستخدم ببريد مستخدم مسبقًا.
- لا يمكن إنشاء جدول متعارض للمعلم أو الطالب.
- فشل إنشاء طالب واحد لا يؤدي إلى حفظ بيانات ناقصة أو رصيد خاطئ دون تنبيه واضح.
- يظهر ملخص كامل للمراجعة قبل التأكيد النهائي.

---

## 5. تصنيفات المعلمين والطلاب

**الحالة:** مكتمل — منفذ ومختبر (انظر "متابعة تنفيذ الجزء الأول")  
**النوع:** تطوير

### التصنيفات المطلوبة

- أطفال.
- ناشئون.
- كبار.
- رجال.
- نساء.
- جميع الفئات.

### تخصصات التدريس

- حفظ.
- تجويد.
- قراءة ونظر.
- لغة عربية.
- قرآن كريم.
- تخصصات أخرى تعتمدها الإدارة.

### قواعد العمل

- الفئة العمرية/النوع منفصلة عن تخصص التدريس.
- يمكن ربط المعلم بأكثر من فئة مناسبة.
- تدعم صفحات الإدارة البحث والفلترة بالفئة والتخصص.
- تظهر الفئات المناسبة أثناء البحث عن معلم لطالب جديد.

---

## 6. دمج وتطوير نظام الجداول

**الحالة:** مكتمل — توليد الجدول الدوري وربطه بطلب الإسناد منفذ ومختبر (انظر "متابعة استكمال الجزء الأول وتنفيذ الجداول والإسناد"). لم يُنفذ ضمن هذا الجزء: تعديل جماعي للحصص المستقبلية مع معاينة، واستمرار الجدول التلقائي عند التجديد (بند §12 منفصل)  
**النوع:** تطوير وربط؛ أصل الجدولة الأسبوعية والشهرية موجود بالفعل

### المطلوب

- إضافة جدول الطالب أثناء إنشاء حسابه أو إسناده إلى المعلم.
- استخدام نظام الجداول الأسبوعية والشهرية والدورية الموجود.
- توليد الحصص للشهور التالية تلقائيًا دون طلب يدوي متكرر.
- عند تعديل الأيام أو الموعد أو مدة الحصة، يُطبق التعديل على الحصص المستقبلية فقط.
- الاحتفاظ بسجل الحصص السابقة دون تغيير.
- إدارة جداول جميع طلاب المعلم من ملفه الإداري.
- منع تضارب وتكرار الحصص للمعلم والطالب.
- استمرار الجدول عند تجديد الاشتراك بنفس المعلم والموعد دون إنشاء نسخ مكررة.

### شروط القبول

- لا تُعدل أي حصة بدأت أو انتهت عند تعديل القاعدة الدورية.
- التعديل الجماعي للحصص المستقبلية يعرض معاينة قبل التأكيد.
- التوقيت يُحسب وفق المنطقة الزمنية المعتمدة للأكاديمية.

---

## 7. أوقات عمل المعلم والمواعيد الشاغرة تلقائيًا

**الحالة:** مكتمل — إعداد أوقات العمل وحساب المواعيد الشاغرة تلقائيًا (أوقات العمل + الحصص المحجوزة + فترة فاصلة اختيارية + الجداول المحجوزة مؤقتًا) منفذ ومختبر يدويًا في متصفح حي، بما في ذلك حالة معلم جديد لم يُنشأ بعد (حساب محلي من أوقات العمل المُدخلة فقط) — انظر "متابعة استكمال الجزء الأول وتنفيذ الجداول والإسناد"  
**النوع:** إضافة جديدة مبنية على الجداول الحالية

### إعداد أوقات العمل

تستطيع الإدارة أو المعلم — حسب الصلاحية — تحديد أوقات العمل لكل يوم:

- اليوم بالكامل.
- غير متاح في هذا اليوم.
- فترة واحدة، مثال: `10:00 صباحًا – 6:00 مساءً`.
- أكثر من فترة في اليوم، مثال: `10:00 – 2:00` و`6:00 – 12:00`.
- استراحات أو أوقات محظورة داخل اليوم.
- فلتر سريع للفترات:
  - صباحًا.
  - بعد الساعة 10 صباحًا.
  - من 12 ظهرًا حتى 12 منتصف الليل.
  - اليوم بالكامل.
  - فترة مخصصة.

### حساب المواعيد الشاغرة

عند إضافة طالب أو تعديل جدوله:

1. يختار المستخدم المعلم.
2. يحدد مدة الحصة المطلوبة، مثل 30 أو 45 أو 60 أو 90 دقيقة.
3. يحدد الأيام أو الفترة المرغوبة.
4. يحسب النظام تلقائيًا الحصص والجداول المحجوزة للمعلم.
5. يعرض فقط الفترات الشاغرة التي تستوعب مدة الحصة كاملة.
6. عند اختيار الموعد يعيد الخادم التحقق من عدم حدوث تعارض قبل الحفظ.

### ما يجب أخذه في الحساب

- الجداول الدورية النشطة.
- الحصص الفردية أو الاستثنائية.
- الحصص المؤجلة إلى وقت جديد.
- مدة كل حصة، وليس وقت بدايتها فقط.
- أوقات العمل والاستراحات.
- المنطقة الزمنية المعتمدة.
- فترة فاصلة بين الحصص إن اعتمدتها الإدارة.

### واجهة العرض

- عرض أسبوعي واضح للأيام والساعات.
- الأوقات المحجوزة بلون مختلف.
- الأوقات الشاغرة قابلة للاختيار.
- بيان سبب عدم توفر الموعد عند الحاجة.
- تحديث المواعيد فور اختيار معلم أو مدة مختلفة.

---

## 8. تسجيل اشتراك ورصيد الطالب الحالي

**الحالة:** مكتمل — منفذ ومختبر (انظر "متابعة تنفيذ الجزء الأول")؛ ربط الاشتراك بجدول دوري مؤجل للجزء الثاني  
**النوع:** تطوير لنظام الاشتراكات والمحفظة الموجود

### المطلوب

- اختيار الباقة من الباقات المفعلة في المنصة.
- جلب إجمالي عدد الحصص تلقائيًا من الباقة المختارة.
- إدخال أحد الرقمين:
  - عدد الحصص المستخدمة، أو
  - عدد الحصص المتبقية.
- حساب الرقم الآخر تلقائيًا.
- منع أن يتجاوز المستخدم أو المتبقي إجمالي حصص الباقة.
- تحديد تاريخ بداية الاشتراك.
- ربط الاشتراك بالمعلم والجدول.
- تسجيل الرصيد كرصيد افتتاحي موثق في محفظة الطالب.

### مثال

باقة 10 حصص + استخدم الطالب 6 حصص = الرصيد الافتتاحي المتبقي 4 حصص.

---

## 9. حساب راتب المعلم بالساعة

**الحالة:** قيد التنفيذ — حقل سعر الساعة وحمايته الصلاحياتية وسجل العمليات عند تعديله مكتمل؛ معادلة حساب قيمة الحصة (سعر الساعة × مدة الحصة ÷ 60) وربطها بسجل الرواتب لم تُنفذ بعد (انظر "متابعة تنفيذ الجزء الأول")  
**النوع:** تطوير؛ حقل سعر الساعة موجود لكن الحساب الحالي يعتمد سعرًا ثابتًا للحصة

### قاعدة الحساب

`قيمة الحصة = سعر الساعة × مدة الحصة بالدقائق ÷ 60`

### أمثلة

- 30 دقيقة = نصف سعر الساعة.
- 60 دقيقة = سعر ساعة كامل.
- 90 دقيقة = سعر ساعة ونصف.

### المطلوب حفظه

- سعر الساعة وقت تنفيذ الحصة.
- مدة الحصة المحتسبة.
- قيمة الحصة.
- حالة الاستحقاق وسببها.
- أي تعديل إداري لاحق وسببه ومنفذه.
- إجمالي المستحقات الشهرية.

---

## 10. المكافآت والخصومات والتسويات

**الحالة:** مطلوب  
**النوع:** تطوير وإضافة

### الطالب

- إضافة أو خصم حصة.
- منح حصة تعويضية.
- تجميد أو استرجاع الرصيد.
- إظهار الرصيد قبل وبعد العملية.
- تسجيل السبب والمنفذ والتاريخ.

### المعلم

- مكافأة مالية.
- خصم أو جزاء.
- تسوية مالية يدوية.
- سبب الحركة ومنفذها.
- صافي المستحق الشهري.

### الموظف

- مكافآت وخصومات.
- سلف وأقساط بسيطة.
- تسويات مالية.
- صافي المستحق وسجل الحركات.

### حدود النطاق

المطلوب نظام مالي إداري بسيط، وليس نظام محاسبة أو موارد بشرية متكاملًا.

---

## 11. الملف الإداري المتكامل للمعلم

**الحالة:** قيد التنفيذ — الملف الإداري الأساسي (البيانات، التخصصات والفئات، سعر الساعة، أوقات العمل، الطلاب المسندون وباقاتهم وأرصدتهم، إضافة طالب) مكتمل؛ التقارير الشهرية والرواتب المحسوبة والمكافآت/الخصومات وتقارير الحلقات خارج نطاق الجزء الأول  
**النوع:** تطوير لملف المعلم الحالي

يستطيع الأدمن مشاهدة وإدارة:

- بيانات المعلم وتخصصاته وفئاته وأوقات عمله.
- الطلاب المسندين إليه.
- اشتراك ورصيد كل طالب.
- جداول الطلاب والمواعيد الشاغرة.
- الحصص السابقة والقادمة.
- تقارير حلقات الطلاب.
- حضور وتأخير وغياب المعلم.
- الأداء والتقييمات.
- تفاصيل الراتب والمكافآت والخصومات.
- إضافة أو نقل طالب.
- تعديل بيانات وجداول الطلاب.

---

## 12. تجديد الاشتراك

**الحالة:** مطلوب  
**النوع:** تطوير؛ التجديد موجود في الخلفية ويحتاج استكمال تجربة الاستخدام

### المطلوب

- تجديد نفس الباقة.
- الاستمرار مع نفس المعلم.
- الاحتفاظ بنفس الجدول والموعد.
- إمكانية تغيير المعلم أو الموعد.
- رفع واعتماد إثبات الدفع.
- إضافة الحصص الجديدة إلى الرصيد.
- الحفاظ على أي رصيد سابق.
- منع تكرار الجداول والحصص.
- إرسال إشعار للطالب والمعلم بعد الاعتماد.

### خارج النطاق

- الخصم البنكي التلقائي.
- بوابة الدفع الإلكتروني، ما لم تعتمد كبند مستقل.

---

## 13. تقرير الحلقة القرآنية

**الحالة:** مطلوب  
**النوع:** تطوير لنموذج إنهاء الحصة والتقييم والواجب الموجود

### محتوى التقرير

- اسم المعلم والطالب.
- تاريخ ومدة ورقم الحصة.
- حضور أو غياب أو تأخير الطالب.
- تقييم مستوى الطالب.
- التسميع والحفظ.
- المراجعة والتجويد.
- النشاط التفاعلي والتربية القرآنية.
- واجب الحصة القادمة.
- ملاحظات المعلم.
- رابط المصحف أو قائمة التشغيل.

### المخرجات

- يظهر للطالب والإدارة.
- يُحفظ في السجل الأكاديمي.
- يمكن طباعته أو مشاركته كرسالة منظمة.

---

## 14. استبيان التقييم والتجديد

**الحالة:** مطلوب  
**النوع:** إضافة جديدة

يشمل:

- تقييم التزام وأداء المعلم.
- تقييم الأكاديمية والمتابعة والتقارير.
- قياس تطور الطالب.
- الملاحظات والمقترحات.
- التوصية بالأكاديمية.
- الرغبة في التجديد.
- الاستمرار مع نفس المعلم أو تغييره.
- طلب تواصل من الإدارة.

تظهر النتائج للإدارة وتتحول إلى متابعة تجديد عند الحاجة.

---

## 15. متابعة التقارير والحصص الشهرية

**الحالة:** مطلوب  
**النوع:** تطوير وإضافة؛ بعض عدادات الحصص موجودة حاليًا بصورة جزئية

### المطلوب

- تذكير المعلم بالتقارير غير المرسلة.
- عرض عدد وأسماء الطلاب أصحاب التقارير الناقصة.
- عرض تاريخ وموعد الحصة ناقصة التقرير.
- فتح الحصة مباشرة لاستكمال التقرير.
- استمرار التنبيه حتى الإرسال.
- رسالة نجاح عند إرسال جميع تقارير اليوم.
- متابعة الإدارة للمعلمين المتأخرين.
- عرض المجدول والمنفذ والمكتمل بالتقرير والمتبقي والملغي شهريًا.
- عرض نسبة الإنجاز مثل `30 من 60`.

### تعريف الحالات

- **تم تنفيذ الحصة:** انتهت الحصة وسُجل الحضور.
- **تم استكمال الحصة:** انتهت الحصة وأُرسل تقرير الطالب.

---

## 16. التقرير الشهري للمعلم

**الحالة:** مطلوب  
**النوع:** إضافة جديدة مبنية على بيانات موجودة

### محتوى التقرير

- المجدول والمنفذ والمكتمل والملغي والمؤجل.
- التقارير المرسلة والناقصة.
- الحضور والالتزام بالمواعيد.
- الطلاب المسندون وملخص متابعتهم.
- قيمة الحصص المستحقة.
- المكافآت والخصومات والتسويات.
- صافي المستحق.
- ملاحظات المعلم والتحديات والاقتراحات.

### دورة التقرير

- يُنشأ كمسودة في نهاية الشهر.
- يراجعه المعلم ويضيف ملاحظاته.
- يرسله إلى الإدارة.
- تراجعه الإدارة وتطلب استكماله أو تعتمده.
- تُحفظ تقارير الشهور السابقة.

### الحالات

- مسودة.
- تم الإرسال.
- يحتاج استكمالًا.
- تمت مراجعة الإدارة.
- معتمد.

---

## 17. إسناد الطالب وقبول/رفض المعلم

**الحالة:** مكتمل — دورة طلب الإسناد الكاملة (مسودة/بانتظار الموافقة/مقبول/مرفوض/تعديل موعد/أُعيد إسناده/مكتمل/ملغي) منفذة ومختبرة يدويًا بالكامل: طالب قديم إسناد مباشر، طالب جديد بانتظار موافقة، رفض مع سبب، تعديل وإعادة إرسال، قبول وتفعيل — راجع "متابعة استكمال الجزء الأول وتنفيذ الجداول والإسناد". الاعتماد الفوري وإعادة الإسناد لمعلم آخر مختبران آليًا (24 اختبار) لكن دون تكرار في متصفح حي هذه الجلسة  
**النوع:** إضافة جديدة جزئيًا؛ قبول الحصة المنفردة موجود لكن قبول إسناد طالب كامل غير موجود

### تحديد نوع الطالب

#### الطالب القديم

- تختار الإدارة أنه `طالب قديم`.
- يتم ربطه مباشرة بالمعلم والاشتراك والجدول.
- لا يحتاج إلى دورة قبول المعلم، إلا إذا اختارت الإدارة إرسال الطلب للمراجعة.

#### الطالب الجديد

- تختار الإدارة أنه `طالب جديد`.
- يُنشأ طلب إسناد للمعلم بدل التفعيل النهائي المباشر.
- لا يبدأ الجدول نهائيًا قبل موافقة المعلم أو اعتماد الإدارة المباشر.

### بيانات طلب الإسناد

- اسم الطالب.
- العمر أو الفئة العمرية.
- المنهج أو التخصص.
- الأيام والمواعيد.
- مدة الحصة.
- نوع التدريس.
- تاريخ بداية الدراسة.
- ملاحظات الإدارة.

### قالب الرسالة

ينشئ النظام الرسالة تلقائيًا، مع السماح للإدارة بمراجعتها وتعديل محتواها قبل الإرسال:

> السلام عليكم ورحمة الله وبركاته 🌷  
> حفظكم الله ورعاكم 🤍  
> نحيطكم علمًا بأنه تمت إضافة طالب/طالبة جديدة إلى جدول حضرتكم، وبياناتها كالتالي:  
> **بيانات الطالب:** الاسم، العمر، المنهج.  
> **بيانات الحلقة:** الأيام، الموعد، مدة الحصة، نوع التدريس.  
> نرجو مراجعة البيانات واختيار الموافقة أو الرفض أو اقتراح موعد آخر.

### خيارات المعلم

- الموافقة على الطالب والجدول.
- رفض الطلب مع سبب إلزامي.
- طلب تعديل الموعد واقتراح وقت بديل.
- إضافة ملاحظات.

### عند الموافقة

- يُفعّل إسناد الطالب للمعلم.
- يُفعّل الجدول والحصص.
- يتم إشعار الإدارة والطالب.
- ينتقل الطلب إلى `مكتمل` ويخرج من قائمة الطلبات المفتوحة.

### عند الرفض أو طلب التعديل

- يصل إشعار فوري للإدارة.
- ينتقل الطلب إلى قائمة `مرفوض/يحتاج إعادة إسناد`.
- يظهر سبب الرفض أو الموعد المقترح.
- تستطيع الإدارة تعديل الموعد وإعادة إرسال الطلب لنفس المعلم.
- أو اختيار معلم آخر وإرسال طلب جديد.
- تستمر الدورة حتى موافقة معلم أو إلغاء الإدارة للطلب.

### حالات الطلب

- مسودة لدى الإدارة.
- بانتظار موافقة المعلم.
- مقبول.
- مرفوض.
- يحتاج تعديل موعد.
- أعيد إسناده.
- مكتمل.
- ملغي.

### ظهور الطلب

- يظهر في إشعارات المعلم.
- يظهر في صندوق منظم باسم `طلبات الطلاب`.
- يظهر للإدارة في قوائم: المعلقة، المقبولة، المرفوضة، وإعادة الإسناد.
- يمكن فتح الطلب كاملًا من الإشعار مباشرة.

---

## 18. الإشعارات والصلاحيات وسجل العمليات

**الحالة:** قيد التنفيذ — الصلاحيات وسجل العمليات وإشعارات الجزء الأول والثاني (إسناد/قبول/رفض/تعديل موعد/إعادة إسناد/اعتماد فوري/إلغاء/تفعيل) مكتملة ومختبرة حيًا (نوع إشعار جديد `assignment` + صلاحيات `assignments.view/manage/override` الجديدة)؛ بنود الرواتب والمكافآت والتقارير الأخرى (§6–§7، §9–§13، §16) لا تزال مطلوبة  
**النوع:** تطوير مشترك لكل البنود

- إشعارات للطالب والمعلم والإدارة حسب الحدث.
- صلاحيات منفصلة للمشاهدة والتنفيذ والاعتماد.
- تسجيل منفذ وتاريخ كل عملية.
- تسجيل سبب التعديلات المالية والإدارية.
- حفظ القيمة أو الرصيد قبل وبعد التعديل.
- تسجيل قبول/رفض وإعادة إسناد الطلاب.
- حماية بيانات الرواتب والأرصدة والاشتراكات.
- اختبار الأنظمة قبل النشر.

---

## متابعة تنفيذ الجزء الأول

> **آخر تحديث:** 2026-08-25 — تنفيذ كامل لنطاق الإصدار التشغيلي الأول (§3) من الخادم إلى الواجهة، مع اختبارات ومراجعة أمنية. راجع `PROJECT_STATUS.md` (قسم "Phase 2 Part 1")، `ARCHITECTURE_PLAN.md` (قسم "Phase 2 Part 1") و`FEATURE_TRACKER.md` للتفاصيل التقنية الكاملة — هذا القسم للمتابعة التشغيلية فقط.

الحالات: `[ ]` لم يبدأ · `[-]` قيد التنفيذ · `[x]` منفذ ومختبر بالكامل · `[!]` محظور مع السبب.

### 1. تصنيفات المعلمين والطلاب
- [x] فصل الفئة العمرية/النوع (`audienceCategories`) عن تخصص التدريس (`specializations`) في القاعدة والـ API وحقول الإدارة.
- [x] دعم أكثر من فئة وأكثر من تخصص لكل معلم.
- [x] الحفاظ على التوافق العكسي — الحقل القديم `category` يبقى متزامنًا تلقائيًا مع `specializations[0]`.
- [x] بحث وفلترة الإدارة بالتخصص والفئة (`GET /admin/teachers`, `GET /teachers/public`).
- [x] اختبارات: قيم غير صالحة مرفوضة، التوافق العكسي محفوظ (`admin.teacher.controller.test.js`).

### 2. أوقات عمل المعلم
- [x] نموذج بيانات جديد `TeacherWorkingHours` لكل يوم: اليوم بالكامل / غير متاح / فترة أو أكثر مخصصة.
- [x] التحقق من صحة الفترات (تنسيق، ترتيب البداية/النهاية، عدم التداخل) في الخادم والواجهة معًا.
- [x] المنطقة الزمنية المعتمدة للأكاديمية كإعداد قابل للتغيير (`AcademySettings.timezone`، الافتراضي `Africa/Cairo`)، بدلاً من قيم مبعثرة في الكود.
- [x] واجهة إدارية لعرض/تعديل أوقات عمل كل معلم (تبويب في ملف المعلم + صفحة الملف الإداري الكامل).
- [ ] حساب المواعيد الشاغرة تلقائيًا اعتمادًا على أوقات العمل والحصص المحجوزة — **مؤجل للجزء الثاني** كما هو منصوص عليه صراحة في نطاق هذا التنفيذ.
- [x] اختبارات: تداخل الفترات، صيغة الوقت، تكرار اليوم (`workingHours.test.js`).

### 3. سعر ساعة التدريس
- [x] الحقل موجود مسبقًا (`User.hourlyRate`)؛ تم تأكيد الحماية الصلاحياتية (`teachers.manage`) وعدم ظهوره في أي واجهة عامة.
- [x] سجل عمليات مخصص (`admin.update_teacher_hourly_rate`) عند أي تعديل، منفصل عن سجل التعديل العام.
- [ ] معادلة حساب قيمة الحصة (سعر الساعة × مدة الحصة ÷ 60) وربطها بسجل الرواتب `TeacherPayrollEntry` — **لم تُطلب ضمن الجزء الأول** (النطاق المطلوب هنا هو الحقل وحمايته فقط)، وتبقى بندًا مفتوحًا لتنفيذ لاحق.

### 4. نوع الطالب (قديم/جديد)
- [x] حقل صريح `User.studentType` بقيمة افتراضية `existing` — لا يغيّر سلوك أي طالب حالي.
- [x] قابل للبحث والفلترة في لوحة الإدارة.
- [x] لا يوجد تنفيذ لدورة قبول/رفض المعلم لإسناد الطالب الجديد — **مؤجل للجزء الثاني** كما هو منصوص عليه صراحة.

### 5. إنشاء معلم مع طلابه من مكان واحد
- [x] تدفق إداري واحد (`POST /admin/onboarding/teacher-with-students`): حساب المعلم + الملف المهني + أوقات العمل + عدد غير محدود من الطلاب، كل طالب بحساب مستقل ونوع وباقة ورصيد افتتاحي.
- [x] رفض البريد الإلكتروني المكرر (داخل نفس الطلب وضد قاعدة البيانات) قبل أي كتابة.
- [x] قائمة سماح صريحة لكل حقل مُدخل (لا يُمرَّر `req.body` كاملاً لأي نموذج).
- [x] عند فشل أي خطوة، تراجع تعويضي كامل (حذف كل ما أنشأه هذا الطلب فقط) بدل ترك بيانات ناقصة.
- [x] حماية من التكرار عند إعادة الإرسال عبر `clientRequestId` — إعادة الإرسال بعد نجاح سابق تُعيد نفس النتيجة بدل إنشاء نسخة جديدة.
- [ ] اختيار موعد شاغر وإنشاء الجدول الدوري ضمن نفس التدفق — **مؤجل للجزء الثاني**.
- [x] اختبارات: بريد مكرر، إنشاء معلم بطالب واحد، إنشاء معلم بعدة طلاب، تراجع عند فشل جزئي، إعادة الإرسال الآمنة (`onboarding.service.test.js`, `adminOnboarding.controller.test.js`).

### 6. اختيار الباقة والرصيد الافتتاحي
- [x] جلب الباقات المفعّلة الحقيقية من القاعدة (`GET /packages`).
- [x] إدخال أحد الرقمين (المستخدم أو المتبقي) واحتساب الآخر تلقائيًا، مع رفض أي قيمة سالبة أو أكبر من إجمالي الباقة.
- [x] تسجيل الرصيد الافتتاحي كحركة موثقة في محفظة الطالب (نوع جديد `opening_balance`) — رصيد قبل/بعد، السبب، المنفذ، التاريخ، كلها محفوظة.
- [x] حماية من الاحتساب المزدوج عند إعادة الإرسال (نفس مفتاح idempotency المستخدم في نظام المحفظة الحالي).
- [ ] توليد جدول الحصص الدوري الكامل — **مؤجل للجزء الثاني**.
- [x] اختبارات: كل حالات `computeOpeningBalance` الصحيحة والخاطئة (`lessonPolicy.test.js`), التراجع عند فشل ائتمان المحفظة (`subscription.service.test.js`).

### 7. الملف الإداري الأساسي للمعلم
- [x] صفحة إدارية جديدة (`/admin/teachers/:id`): البيانات الأساسية، التخصصات والفئات، سعر الساعة، أوقات العمل، الطلاب المسندون مع باقة كل طالب ورصيده الحي، إضافة طالب لاحقًا.
- [x] حالات تحميل/خطأ/فارغة حقيقية (لا بيانات وهمية).
- [x] متجاوب (320/768/1024/1440px)، متوافق مع RTL، تباين كافٍ، بدون اعتماد على اللون وحده كمؤشر حالة.
- [ ] التقارير الشهرية، الرواتب المحسوبة، المكافآت/الخصومات، تقارير الحلقات — **خارج نطاق الجزء الأول** كما هو منصوص عليه صراحة.

### 8. الصلاحيات وسجل العمليات
- [x] كل نقاط النهاية الجديدة محمية بـ `requirePermission('teachers.manage', 'students.manage')` حسب العملية — لا فحص صلاحيات على الواجهة فقط.
- [x] سجل عمليات لكل عملية حساسة: إنشاء/تعديل معلم، تعديل سعر الساعة، تعديل أوقات العمل، إنشاء طالب، إنشاء اشتراك، تسجيل الرصيد الافتتاحي، إضافة طالب من ملف المعلم.
- [x] لا كلمات مرور أو رموز دخول أو أسرار في أي سجل عمليات.
- [x] تصحيح ثغرة تعيين جماعي (mass assignment) كانت موجودة مسبقًا في `createTeacher` (كان يمرر `req.body` كاملاً لـ `User.create` دون قائمة سماح) — أصلحت ضمن هذا التنفيذ.
- [ ] إشعارات مخصصة لأحداث الجزء الأول الجديدة (إنشاء حساب معلم/طالب) — لم تُضف بعد، بخلاف إشعار تفعيل الاشتراك الموجود مسبقًا والمعاد استخدامه.

### التحقق والاختبار المنفذ
- `cd server && npx jest` → 243/245 ناجح (فشلان قديمان غير مرتبطان بهذا التنفيذ، موثقان منذ 2026-07-16).
- `node -e "require('./src/routes/index.js')"` → كامل شجرة المسارات يُحمَّل دون أخطاء.
- `cd client && npm run build` → صفر أخطاء.
- لم يُنفَّذ اختبار متصفح حي (لا توجد خوادم تطوير قيد التشغيل في هذه البيئة) — يوصى بجولة تحقق يدوية قبل الإطلاق الفعلي، خصوصًا مسار المعالج الكامل والتراجع عند الفشل.

### قيود معروفة (مقصودة، ليست إغفالًا)
- ~~محرك حساب المواعيد الشاغرة التلقائي غير مبني بعد~~ — **نُفذ في الجزء الثاني**، انظر القسم التالي.
- ~~توليد الجدول الدوري الكامل عند إضافة طالب غير مبني بعد~~ — **نُفذ في الجزء الثاني**.
- ~~دورة قبول/رفض المعلم لإسناد طالب جديد غير مبنية بعد~~ — **نُفذت بالكامل في الجزء الثاني**.
- تعديل المعلم لأوقات عمله بنفسه غير متاح — النطاق المطلوب صراحة في الجزء الأول هو "المسؤولون المخوّلون" فقط؛ هذا قرار مفتوح لا يزال قائمًا (انظر §19 أدناه).

---

## متابعة استكمال الجزء الأول وتنفيذ الجداول والإسناد

> **آخر تحديث:** 2026-08-25 — تنفيذ كامل لخيارات بيانات دخول الطالب، محرك المواعيد الشاغرة، تفعيل الجدول الدوري من المعالج، ودورة طلب الإسناد الكاملة (قبول/رفض/تعديل موعد/إعادة إسناد/اعتماد فوري)، من الخادم إلى الواجهة، مع اختبارات آلية ومراجعة يدوية حية في متصفح فعلي ضد قاعدة بيانات حقيقية. راجع `PROJECT_STATUS.md` (قسم "Phase 2 Part 2")، `ARCHITECTURE_PLAN.md` (قسم "Phase 2 Part 2") و`FEATURE_TRACKER.md` للتفاصيل التقنية الكاملة — هذا القسم للمتابعة التشغيلية فقط.

الحالات: `[ ]` لم يبدأ · `[-]` قيد التنفيذ · `[x]` منفذ ومختبر بالكامل · `[!]` محظور مع السبب.

### 1. خيارات بيانات دخول الطالب (§1–§2 من طلب التنفيذ)
- [x] وضع تلقائي آمن (يعيد استخدام النظام الحالي دون تغيير) ووضع يدوي بكلمة مرور يحددها المسؤول، مع تأكيد وإظهار/إخفاء (`config/passwordPolicy.js`, `config/credentialMode.js`).
- [x] رفض كلمة المرور الضعيفة وعدم التطابق على الخادم دائمًا، بصرف النظر عن تحقق الواجهة.
- [x] خيار "مطالبة بتغيير كلمة المرور عند أول دخول" منفصل عن وضع بيانات الدخول، افتراضيًا مفعّل.
- [x] لا كلمة مرور نصية تُحفظ أو تُسجَّل أو تُعاد في الاستجابة العادية أو المراجعة النهائية — التحقق اليدوي الحي أكد ذلك.
- [x] مطبّق في: معالج إنشاء المعلم مع طلابه، إضافة طالب من ملف المعلم، إنشاء طالب مستقل.
- [x] اختبارات: `passwordPolicy.test.js`, `credentialMode.test.js`, واختبارات تكامل في `onboarding.service.test.js`.
- [x] تحقق حي: تسجيل دخول فعلي بكلمة مرور محددة يدويًا + ظهور شاشة "مطلوب تغيير كلمة المرور" الإلزامية + نجاح التغيير.

### 2. محرك حساب المواعيد الشاغرة تلقائيًا (§4، §7)
- [x] `services/availability.service.js`: يحسب الفترات الشاغرة من أوقات العمل مطروحًا منها الجداول الدورية والحصص الفعلية والطلبات المحجوزة مؤقتًا، مع مراعاة مدة الحصة الكاملة والفترة الفاصلة الاختيارية (`AcademySettings.lessonBufferMinutes` الجديد).
- [x] إعادة تحقق آمرة على الخادم (`checkAvailability`) قبل أي كتابة فعلية — لا يُعتمد على معاينة الواجهة أبدًا.
- [x] حالة المعلم الجديد غير المُنشأ بعد (داخل المعالج): حساب محلي في الواجهة من أوقات العمل المُدخلة فقط، دون طلب خادم.
- [x] تحقق حي: تقسيم يوم عمل كامل بشكل صحيح حول حصة محجوزة فعليًا (مثال حقيقي: `00:00–16:00، 16:30–24:00`).
- [x] اختبارات: `availability.service.test.js` (نافذة كاملة اليوم، يوم غير متاح، تعارض جدول دوري، فترة أضيق من المدة، الفترة الفاصلة).

### 3. دورة طلب الإسناد الكاملة (§6–§11)
- [x] نموذج `AssignmentRequest` وآلة الحالات الصريحة (`config/assignmentStatus.js`) بكل الحالات المطلوبة والانتقالات المسموحة فقط.
- [x] طالب قديم أو اعتماد فوري مُصرَّح به (صلاحية `assignments.override` الجديدة + سبب إلزامي + سجل عمليات) → تفعيل مباشر بدون انتظار المعلم.
- [x] طالب جديد بدون اعتماد فوري → طلب معلّق يظهر في "طلبات الطلاب" لدى المعلم.
- [x] قبول المعلم → إعادة تحقق من التوفر ثم تفعيل جدول دوري حقيقي وحصص محدودة العدد (نفس نظام التوليد الحالي).
- [x] رفض المعلم (بسبب إلزامي) واقتراح موعد بديل، مع إشعار فوري للإدارة.
- [x] تعديل الإدارة للجدول وإعادة الإرسال لنفس المعلم، أو إعادة الإسناد لمعلم آخر (طلب جديد مرتبط، مع حفظ التاريخ الكامل)، أو الإلغاء.
- [x] رسالة الإسناد الافتراضية بالعربية، قابلة للتعديل، مع صياغة تراعي النوع فقط عند وجود بيانات موثوقة.
- [x] إشعارات لكل حدث (طلب جديد، قبول، رفض، تعديل موعد، إعادة إرسال، إعادة إسناد، اعتماد فوري، إلغاء، تفعيل) — نوع إشعار جديد `assignment`.
- [x] تحقق حي كامل: إنشاء طلب معلّق → رفض بسبب → تعديل وإعادة إرسال → قبول → تفعيل، مع وصول الإشعارات الفعلية في كل خطوة، صفر أخطاء في الطرفية.
- [x] اختبارات: `assignmentStatus.test.js`, `assignmentMessage.test.js`, `assignment.service.test.js` (24 اختبارًا تغطي كل الانتقالات والاعتماد الفوري وإعادة الإسناد والإلغاء).
- [x] واجهة المعلم: `طلبات الطلاب` (`TeacherAssignmentRequestsPage.jsx`) مع شارة عدد معلّق في القائمة الجانبية.
- [x] واجهة الإدارة: `متابعة طلبات إسناد الطلاب` (`AdminAssignmentRequestsPage.jsx`) مع شارة عدد يحتاج متابعة، وربط مباشر من الإشعار للطلب.

### 4. إعادة تصميم واجهة جدولة الطالب المضغوطة (طلب تحسين UX لاحق، 2026-08-25)
> استُخدمت مهارة `ui-ux-pro-max` قبل التنفيذ. استبدلت هذه الإعادة التصميمية عرض الأيام السبعة الدائم القديم بمعالج جدولة مضغوط، مع الحفاظ الكامل على محرك المواعيد الشاغرة الحقيقي ودورة الإسناد الموجودين — تصميم فقط، لا محرك مواز جديد.

- [x] بناء جدولة مضغوط: تخصص → مدة → نمط تكرار → أيام (عند الحاجة) → مواعيد → تاريخ بداية/نهاية → مراجعة أسبوعية/شهرية، بدل سبعة صفوف دائمة.
- [x] أربعة أنماط تكرار مدعومة فعليًا من محرك الجدولة: يوميًا، أسبوعيًا، كل أسبوعين، شهريًا — شرائح مقروءة بأيقونات ووصف قصير، لا نص تقني.
- [x] شرائح أيام أسبوع مضغوطة (`WeekdayChipSelector.jsx`) بأزرار حقيقية، `aria-label` باسم اليوم الكامل، وحالات مميزة (محدد/متعارض) بلون + أيقونة معًا وليس اللون وحده.
- [x] صفوف وقت لكل يوم مُختار فقط (لا صفوف للأيام غير المختارة)، مع وقت نهاية محسوب تلقائيًا، "تطبيق على الجميع"، ونسخ لكل صف، وإزالة مباشرة.
- [x] منتقي مواعيد ذكي (`ScheduleSlotPicker.jsx`) يحمّل الفترات الشاغرة الحقيقية من محرك التوفر نفسه، مقسّمة صباحًا/ظهرًا وعصرًا/مساءً، مع هيكل تحميل (skeleton) وحالة فارغة وتنبيه عند عدم توفر الموعد المختار.
- [x] نصوص عربية واضحة بدل الصيغة التقنية الخام: "متاح طوال اليوم"، "متاح من 10:00 ص إلى 2:00 م"، "لا توجد مواعيد تناسب مدة الحصة"، "الموعد محجوز" — لا يظهر `00:00–24:00` في أي مكان.
- [x] جدولة مقترحة (`buildSuggestions`): أقرب موعد متاح، نفس الموعد في يوم آخر، نفس الأيام بموعد مختلف — من حسابات توفر حقيقية، قابلة للتطبيق بنقرة واحدة والتعديل بعدها.
- [x] تصحيح تاريخ النهاية: يجب أن يكون بعد تاريخ البداية دائمًا، مع خيار صريح "يستمر حتى الإلغاء"، تنسيق عربي واضح للتاريخ (غير معتمد على لغة المتصفح)، أخطاء ضمنية بجانب الحقل، ومنع المتابعة للمراجعة أثناء وجود خطأ.
- [x] بطاقة "الجدول الأسبوعي" وقسم "معاينة الشهر الأول" القابل للطي — يعكس قاعدة التكرار الفعلية، ويحدَّث فور تغيير أي اختيار.
- [x] ملخص مضغوط في رأس بطاقة الطالب المطوية، مثل: "الأحد والثلاثاء • 6:00 م • 30 دقيقة • أسبوعيًا".
- [x] فصل بصري بين شارة مسار الإسناد (قديم/جديد/اعتماد فوري) وعناصر التحكم في الجدول نفسه.
- [x] **إضافة خلفية مبررة:** دعم تكرار "يوميًا" في `AssignmentRequest.schedule.frequency` و`validateSchedulePayload` (كان يدعمه محرك `schedule.service.js` أصلاً دون واجهة)، وإصلاح تكرار "شهريًا" ليُنشئ قاعدة `ScheduleRule` بـ`daysOfWeek` فارغ (تكرار على نفس يوم الشهر فعليًا، لا كل أسبوع كما كان سيحدث لو مرّرنا يوم الأسبوع).
- [x] اختبارات وحدة جديدة بالكامل من جهة العميل (`vitest` أُضيف كتبعية تطوير خفيفة لعدم وجود بنية اختبار سابقة) — 41 اختبارًا لكل منطق الجدولة النقي، بالإضافة إلى 5 اختبارات خلفية جديدة لتكرار "يوميًا"/"شهريًا".
- [x] تحقق يدوي حي كامل في متصفح فعلي: بناء جدول أسبوعي بيومين، تبديل لأنماط يومي/شهري، رفض موعد نهاية قبل موعد البداية، معاينة شهرية بـ9 حصص حقيقية، إرسال الويزارد بالكامل ونجاح التفعيل الفعلي بجدول جديد يعكس البناء المضغوط.

**خطأ حقيقي اكتُشف وأُصلح أثناء هذا التحقق الحي (لم يظهر في أي اختبار آلي مسبق):** توفر يوم كامل (`00:00–24:00`) كان يُحسب محليًا (لمعلم لم يُنشأ بعد داخل الويزارد) كنافذة صفرية الطول بسبب دالة تنسيق وقت جديدة تُرجع `00:00` بدل `24:00` عند التفاف الدقائق — ما كان يجعل كل الأوقات تظهر "محجوزة" رغم توفر المعلم طوال اليوم فعليًا. اكتُشف بمعاينة حية للنص المعروض، وأُصلح بدالة تنسيق حدود منفصلة (`minutesToBoundaryTimeStr`) تحافظ على `24:00` كقيمة حدّية، مع بقاء الدالة العامة الأخرى صالحة لحساب الالتفاف الحقيقي (مثل 23:45 + 30 دقيقة = 00:15 اليوم التالي).

### أخطاء حقيقية اكتُشفت وأُصلحت أثناء المراجعة اليدوية الحية (وليس بالمراجعة الكودية فقط)
- رسالة الإسناد كانت تعرض `[object Object]` بدلاً من أسماء الأيام بسبب خطأ منطقي في `config/assignmentMessage.js` — تم اكتشافه بمعاينة رسالة حقيقية في حساب معلم حقيقي، وأُصلح، وأُضيف اختبار انحدار مخصص.
- "الطلاب المسندون" في الملف الإداري للمعلم لم يكن يعرض طالبًا له جدول دوري نشط بدون اشتراك/باقة بعد (حالة مدعومة صراحة في هذا التنفيذ) — تم اكتشافه بإضافة طالب فعلي عبر الواجهة والتحقق من ظهوره، وأُصلح بدمج الطلاب المرتبطين عبر `ScheduleRule` في نفس القائمة.

### التحقق والاختبار المنفذ
- `cd server && npx jest` → 325/327 ناجح بعد إضافة اختبارات "يوميًا"/"شهريًا" (فشلان قديمان غير مرتبطان، موثقان منذ 2026-07-16).
- `cd client && npx vitest run` → 41/41 ناجح (اختبارات منطق الجدولة النقي الجديدة بالكامل — `vitest` أُضيف كتبعية تطوير خفيفة، أول بنية اختبار للعميل في هذا المستودع).
- `node -e "require('./src/routes/index.js')"` → كامل شجرة المسارات يُحمَّل دون أخطاء.
- `cd client && npm run build` → صفر أخطاء.
- **تحقق يدوي حي في متصفح فعلي** ضد خادم Node.js وقاعدة MongoDB حقيقيين (وليس فقط طلبات curl): إنشاء معلم بكلمة مرور يدوية + طالب قديم مجدول (تفعيل مباشر، 5 حصص حقيقية تولّدت بالتواريخ الصحيحة)، إضافة طالب جديد بجدول (طلب معلّق)، رفض المعلم بسبب، تعديل الإدارة وإعادة الإرسال، قبول المعلم وتفعيل الجدول، وصول كل الإشعارات فعليًا، عمل محرك المواعيد الشاغرة محليًا وعلى الخادم بشكل صحيح، عمل شاشة إلزام تغيير كلمة المرور وتحريرها. صفر أخطاء في طرفية المتصفح طوال الجلسة.

### قيود معروفة (مقصودة، ليست إغفالًا)
- الاعتماد الفوري وإعادة الإسناد لمعلم آخر مختبران آليًا بالكامل، لكن دون تكرار تحقق يدوي في متصفح حي هذه الجلسة — يوصى بجولة سريعة قبل أول استخدام إنتاجي فعلي لصلاحية الاعتماد الفوري.
- "معاينة أقرب المواعيد" في واجهة الجدول تقدير خفيف من جهة العميل (قائمة تواريخ)، وليست شبكة تقويم كاملة — الحصص الفعلية المحدودة العدد المولَّدة من الخادم هي المرجع الرسمي.
- عدد "الطلاب" في صفحة قائمة المعلمين (وليس ملف المعلم الفردي الذي أُصلح هذه الجلسة) لا يزال مبنيًا على الاشتراكات فقط — بند متابعة منخفض الأولوية.
- لا توجد شاشة إدارية مستقلة لـ"إنشاء إسناد جديد" خارج المعالج/إضافة طالب — الإسناد المباشر والطلبات الجديدة تُنشأ من هاتين الواجهتين، وصفحة الإدارة مخصصة لمتابعة الطلبات القائمة (تعديل/إعادة إسناد/إلغاء) تحديدًا.
- اقتراح المعلم لموعد بديل لا يُحجز مؤقتًا تلقائيًا (سؤال عمل مفتوح في §19 أدناه، لم يُحسم) — فقط الموعد الأصلي المطلوب محجوز أثناء `pending_teacher_approval`.
- لم يتحقق هذا التنفيذ من التخطيط البصري الدقيق بالبكسل على عرض 320/768/1024/1440 عبر لقطات شاشة فعلية لضيق العرض — أداة تغيير حجم النافذة في هذه البيئة لم تُغيّر فعليًا `window.innerWidth` (تحقّق منها مرتين). التحقق من الاستجابة تم عبر مراجعة كودية لفئات Tailwind (تصميم "mobile-first" بفواصل `sm:`، `flex-wrap` على شرائح الأيام، `flex-col sm:flex-row` لصفوف اليوم/الوقت) وفحص الأنماط المحسوبة (`flex-wrap: wrap` مؤكد فعليًا)، وليس عبر مراجعة بصرية حية على عرض هاتف حقيقي — يوصى بجولة يدوية سريعة بمتصفح حقيقي مصغّر قبل الإطلاق.

### 5. إصلاح معمارية الإعداد التدريجي متعدد الطلاب (2026-08-26) — منع تكرار اقتراح المواعيد والحجز المزدوج

**السبب الجذري:** المعالج القديم كان يحفظ المعلم وكل الطلاب دفعة واحدة عند "تأكيد الإنشاء" فقط — أثناء إضافة الطالب الثاني والثالث، كان جدولهم لا يزال في حالة الواجهة المحلية فقط، فمحرك التوفر (`availability.service.js`) لم يكن يرى حجز الطالب الأول بعد، ما يؤدي لاقتراح نفس الموعد أكثر من مرة واحتمال حجز مزدوج فعلي عند الحفظ النهائي.

**الحل المطبَّق — حفظ تدريجي حقيقي:**
- يُحفظ المعلم فوريًا في قاعدة البيانات فور اكتمال بيانات "أوقات العمل" (قبل إضافة أي طالب)، بحالة `onboardingStatus: 'draft'` (مخفي من الدليل العام ومن تسجيل الدخول حتى إنهاء العملية)، عبر جلسة إعداد جديدة قابلة للاستئناف: نموذج `OnboardingSession` (منفصل عمدًا عن `OnboardingRequest` القديم الذي بقي كما هو لغرضه الأصلي — ذاكرة تكرار للمسار القديم دفعة واحدة).
- كل طالب يُحفظ فور الضغط على "حفظ الطالب وإضافة طالب آخر"/"حفظ الطالب والمتابعة للمراجعة": يُنشأ حساب الطالب + الاشتراك/الرصيد الافتتاحي (إن وُجد) + طلب الإسناد وحجز الجدول **في نفس الاستدعاء**، فيراه محرك التوفر فورًا للطالب التالي — لا مجال لاقتراح موعد محجوز فعليًا.
- حماية تزامن حقيقية من طبقة القاعدة نفسها: نموذج جديد خفيف `ScheduleReservationLock` بفهرس فريد `{teacherId, dayOfWeek, time}` يجعل "حجز موعد محدد" عملية ذرية — محاولتان متزامنتان لنفس الموعد بالضبط لا يمكن أن تنجحا معًا؛ الخاسرة تتلقى خطأ 409 برسالة "تم حجز هذا الموعد منذ لحظات. اخترنا لك أقرب المواعيد المتاحة." مع بدائل حقيقية محسوبة من محرك التوفر (`suggestAlternativeSlots` الجديدة في `availability.service.js`)، وليس تخمينًا بإضافة ساعة.
- إزالة طالب من العملية، أو رفض المعلم للطلب، أو اقتراح موعد بديل: تُحرَّر الحجوزة (القفل + حالة الطلب) فورًا حتى تعود متاحة لطالب آخر — تحقق يدويًا حيًا (حذف طالب ثانٍ ثم التأكد من عودة موعده متاحًا لطالب جديد).
- فشل حفظ أي طالب (مثال: تعذّر إنشاء الاشتراك أو حجز الجدول) لا يحذف المعلم ولا أي طالب محفوظ سابقًا — فقط سجلات هذا الطالب نفسه التي أُنشئت في هذا الاستدعاء تُراجع (rollback) بدقة.
- تحديث/إعادة تحميل الصفحة يستأنف الجلسة تلقائيًا (بانر "لديك عملية إعداد سابقة لم تكتمل — هل تريد استئنافها؟") دون تكرار المعلم أو أي طالب محفوظ.
- خطوة "المراجعة النهائية" تُحمَّل من الخادم مباشرة (وليس من حالة الواجهة المحلية)، وتمنع الإنهاء إن وُجد طالب بحالة جدول تحتاج معالجة (رفض/اقتراح موعد آخر). إنهاء العملية (`finalize`) يُحوّل `onboardingStatus` للمعلم من `draft` إلى `complete` — عندها فقط يظهر في الدليل العام ويمكنه تسجيل الدخول.
- المسار القديم دفعة واحدة (`POST /admin/onboarding/teacher-with-students`) بقي فعالًا بالكامل دون أي تغيير في سلوكه — هذه منظومة إضافية موازية، وليست استبدالًا.

**تحقق يدوي حي كامل بثلاثة طلاب فعليين** (السيناريو المطلوب): معلم محفوظ فوريًا → طالب أول محجوز الأحد 12:00 (ساعة كاملة) → إضافة طالب ثانٍ: تأكد أن 12:00 لم يعد ضمن الخيارات إطلاقًا (القائمة تقفز من 11:00 صباحًا إلى 1:00 ظهرًا مباشرة) → حجز الطالب الثاني 1:00 ظهرًا → إضافة طالب ثالث: تأكد أن كلًا من 12:00 و1:00 مستبعدان معًا (القائمة تقفز من 11:00 صباحًا إلى 2:00 ظهرًا) → حجز الطالب الثالث 2:00 ظهرًا → حذف الطالب الثاني من العملية → تأكيد عودة نافذة 1:00–2:00 ظهرًا متاحة من جديد (بينما 12:00–1:00 و2:00–3:00 يبقيان محجوزين) → تحديث الصفحة بالكامل → ظهور بانر الاستئناف → استئناف الجلسة → تأكد بقاء المعلم والطالبين الأول والثالث محفوظين دون أي تكرار → إنهاء العملية بنجاح → تأكد ظهور المعلم في الدليل العام بعد الإنهاء مباشرة. جميع الخطوات نجحت دون أي تعديل إضافي بعد أول تنفيذ.

**اختبارات آلية جديدة:** `availability.service.test.js` (4 اختبارات جديدة لـ`suggestAlternativeSlots` تطابق أمثلة طلب التنفيذ حرفيًا: حجز 30 دقيقة بلا فاصل يقترح الموعد التالي مباشرة، مع فاصل 10 دقائق يؤجل الاقتراح، حجز ساعة كاملة يقترح بداية الساعة التالية، والانتقال ليوم آخر عند عدم وجود مساحة في اليوم المطلوب إطلاقًا)، `assignment.service.test.js` (7 اختبارات جديدة لتكامل `ScheduleReservationLock`: الحجز عند الإنشاء، تعارض القفل المتزامن يُعيد 409 مع بدائل حقيقية دون ترك الطلب معلّقًا، تحرير القفل بعد التفعيل الناجح/الرفض/اقتراح الموعد، وإعادة حجزه عند التعديل وإعادة الإرسال)، و`onboardingSession.service.test.js` (15 اختبارًا جديدًا بالكامل: الحفظ التدريجي المستقل لطالبين متتاليين، إعادة المحاولة بنفس المعرّف لا تُكرر الحفظ، فشل طالب لا يمسّ طالبًا آخر أو المعلم، الحذف يحرر الحجز، الإنهاء متكرر بأمان (idempotent) ويمنع الإكمال عند وجود مشكلة غير محلولة). إجمالي اختبارات الخادم: 351/353 ناجح (فشلان قديمان في `sessionIntelligence.test.js` غير مرتبطين بهذا التنفيذ إطلاقًا، موجودان مسبقًا في الشيفرة الأساسية).

**قيود معروفة (مقصودة):** حماية `ScheduleReservationLock` تمنع تصادم المفتاح الدقيق (نفس اليوم والوقت بالضبط) فقط، وليست حماية كاملة من تداخل فترات زمنية جزئية (مثال: 12:00–13:00 مقابل 12:30–13:30) — نفس القيد الموثّق في `wallet.service.js` بسبب عدم توفر معاملات متعددة المستندات على MongoDB المستقل هنا؛ `checkAvailability()` تبقى تُنفَّذ فورًا قبل أي كتابة لتضييق هذه الفجوة إلى أقصى حد. لا توجد بعد شاشة إدارية مخصصة لعرض/إدارة جلسات الإعداد المعلّقة لعدة معلمين في آنٍ واحد (الاستئناف حاليًا عبر `localStorage` للمتصفح نفسه فقط، وليس عبر قائمة مركزية على الخادم يراها كل المسؤولين) — الخدمة الخلفية (`listOnboardingSessions`) جاهزة لدعم هذه الشاشة لاحقًا دون تغيير معماري إضافي.

### 6. تحسين تنقّل مركّز: متابعة إضافة الطلاب من صفحة نجاح الويزارد (2026-08-26)

تحسين UX ملاحة صغير ومنفصل — وليس إعادة هيكلة إضافية لمعمارية الإعداد التدريجي (القسم 5 أعلاه). المشكلة: بعد نجاح إنشاء المعلم وطلابه الأوائل، كانت صفحة النجاح تعرض فقط "إنشاء معلم آخر" و"الذهاب لقائمة المعلمين" — لا يوجد مسار مباشر لمتابعة إضافة طلاب لنفس المعلم دون الرجوع لقائمة المعلمين والبحث عنه يدويًا.

**الحل:** زر أساسي جديد بأعلى أولوية بصرية "إضافة المزيد من الطلاب لهذا المعلم" يستخدم `teacher._id` الحقيقي من نتيجة الإنهاء (`finalizeOnboardingSession`)، وينقل الأدمن مباشرة إلى `/admin/teachers/:id?action=add` — نفس مسار الملف الإداري للمعلم الموجود مسبقًا (`ADMIN_TEACHER_PROFILE`)، مع علامة استعلام تفتح تلقائيًا نافذة "إضافة طالب" الموجودة أصلًا في تلك الصفحة (`AddStudentModal`) دون إنشاء صفحة أو تدفق مكرر. ترتيب الأزرار أصبح: أساسي (إضافة طلاب لهذا المعلم) → ثانوي (عرض ملف المعلم) → ثانوي/إطاري (الذهاب لقائمة المعلمين) → ثالثي أقل بروزًا (إنشاء معلم آخر). جملة تأكيد مختصرة تظهر قبل الزر الأساسي: "يمكنك الآن إضافة طلاب آخرين إلى أ. {اسم المعلم} دون إعادة إدخال بيانات المعلم."

عند فتح نافذة "إضافة طالب" (سواء من هذا المسار أو من الملف الإداري مباشرة كما كان)، تظهر الآن بطاقة سياق مقفلة (غير قابلة للتعديل) بأعلى النافذة: اسم المعلم، تخصصاته، ملخص أوقات عمله، وعدد الطلاب المسندين حاليًا — لا يوجد ولم يكن هناك أي منتقي معلم قابل للتغيير داخل هذه النافذة أصلًا، فالمتطلب محقَّق بالتصميم القائم. النافذة تستخدم نفس نقطة النهاية الموجودة `POST /admin/teachers/:teacherId/students` وكل الخدمات المُعاد استخدامها (الاشتراك/الرصيد الافتتاحي/بيانات الدخول/الجدولة/الإسناد/الإشعارات/التدقيق) دون أي تغيير خلفي — هذا تحسين واجهة فقط، صفر تغييرات على العقود الخلفية.

مسار الاستئناف من الخادم (`GET /admin/teachers/:id`) يتحقق دائمًا من وجود المعلم فعليًا قبل فتح النافذة تلقائيًا؛ معلم محذوف أو صلاحية غير كافية يُظهران رسالة خطأ واضحة (`ErrorState` الموجودة) بدلًا من فتح نموذج فارغ بصمت. كذلك أُضيف بانر احتياطي خفيف (لا يحتوي بيانات حساسة، فقط معرّف/اسم المعلم في `sessionStorage`) يظهر إذا فُقدت حالة صفحة النجاح بعد تحديث المتصفح، يوفّر رابطًا آمنًا لملف المعلم دون أي إعادة إرسال تلقائية لطلب الإنشاء الأصلي.

**اختبارات جديدة:** `client/src/config/__tests__/constants.test.js` (دالة `buildTeacherAddStudentUrl` — تستخدم معرّف المعلم الحقيقي حصرًا، لا بريد/اسم/فهرس، وتؤول لقائمة المعلمين إن لم يتوفر معرّف)، و`client/src/utils/__tests__/workingHours.test.js` (دالة `summarizeWorkingHoursDays` الجديدة لبطاقة السياق). التحقق: `cd client && npx vitest run` → 47/47 ناجح (41 سابق + 6 جديد)، `npx eslint` نظيف على كل الملفات المعدَّلة، `npm run build` صفر أخطاء. لا تغييرات خلفية في هذا القسم، لذا لم تُعَد اختبارات الخادم (351/353 كما هي).

**قيود معروفة:** لم يُنفَّذ تحقق يدوي حي كامل بمتصفح فعلي لهذا التحسين تحديدًا في هذه الجلسة — إضافة الملحق (Claude-in-Chrome) لم تتصل رغم عدة محاولات إعادة اتصال. التحقق اقتصر على المراجعة الكودية، فحص الأنواع/المسارات، ونجاح البناء/الاختبارات الآلية. يوصى بإجراء جولة يدوية سريعة (السيناريو الموصوف في طلب التنفيذ) قبل أول استخدام إنتاجي فعلي.

### 7. إعادة تصميم "اقتراح موعد بديل" + كتالوج المناهج الديناميكي + تدقيق جاهزية الجزء الأول (2026-08-28)

**أ) إعادة تصميم "اقتراح موعد بديل" للمعلم بالكامل:**

**السبب الجذري للتجربة السيئة:** الواجهة القديمة كانت قائمة يوم `<select>` بسيطة + `<input type="time">` أصلي بلا أي ربط بمحرك التوفر الحقيقي — لا تحقق فوري من التعارض، لا اقتراحات ذكية، تنسيق وقت غير متسق (`05:30 --`)، وقيمة افتراضية `16:00` ثابتة بلا معنى. الأخطر: **الخادم لم يكن يعيد التحقق من الموعد المقترح إطلاقًا** قبل قبوله — طلب `time_change` كان يُقبل بأي يوم/وقت مهما كانت الحالة الفعلية للتوفر.

**الحل المطبَّق:**
- نقطة نهاية جديدة مملوكة للمعلم `GET /teachers/me/assignment-requests/:id/availability` (وليس نقطة نهاية الإدارة العامة) — الملكية مضمونة عبر تحديد النطاق بطلب المعلم نفسه فقط (`findOne({_id, teacherId: req.user._id})`)، تعيد الفترات الشاغرة الحقيقية (`getWeeklyAvailability`) + اقتراحات ذكية حقيقية (`suggestAlternativeSlots`) — نفس محرك التوفر المستخدم في المعالج، لا محرك مواز.
- **إصلاح أمني/منطقي حقيقي:** `respondToAssignment`'s فرع `time_change` أصبح يعيد التحقق الآمر من التوفر (`checkAvailability`) قبل قبول أي اقتراح — موعد أصبح غير متاح يُرفض بخطأ 409 مع تفاصيل التعارض وبدائل حقيقية جديدة (`suggestAlternativeSlots`)، تمامًا كما يحدث عند قبول الطلب.
- مكوّن واجهة جديد بالكامل `ProposeAlternativeTimeModal.jsx`: ملخص الطلب الحالي، قسم "مواعيد مقترحة" (أقرب وقت لنفس اليوم/يوم آخر بنفس الوقت/أقرب موعد عام — بشرح مختصر لكل اقتراح)، شرائح أيام يمكن اختيار واحد منها فقط (تُعطَّل الأيام بلا مواعيد متاحة مع عدّاد للمواعيد المتاحة)، ثم `ScheduleSlotPicker.jsx` الحالي (نفس المكوّن المستخدم في معالج الإعداد) لعرض المواعيد مجمّعة صباحًا/ظهرًا/مساءً بتنسيق عربي 12 ساعة، وجملة تأكيد نهائية قبل الإرسال، مع حفظ خاصية "الملاحظة" الاختيارية.
- عند رفض الخادم لموعد أصبح غير متاح (409)، تُبطَل ذاكرة التخزين المؤقت لاستعلام التوفر فورًا فتُحمَّل بدائل حقيقية جديدة في النافذة المفتوحة نفسها دون إغلاقها.

**ب) كتالوج المناهج التعليمية الديناميكي (بدل القائمة الثابتة):**

نموذج جديد `TeachingSubject` (`server/src/models/TeachingSubject.js`) + خدمة كاملة (`services/teachingSubject.service.js`) بذاكرة تخزين مؤقت قصيرة الأمد. المفاتيح الستة الأساسية (`tajweed`/`hifz`/`nazra`/`arabic`/`quran`/`other`) تُزرع تلقائيًا (`isSystem: true`) بنفس مفاتيحها النصية القديمة — **لا حاجة لأي ترحيل بيانات**، كل سجل قديم في `User`/`Course`/`AssignmentRequest` يبقى صالحًا كما هو. أي منهج جديد (مثل "الرياضيات") يُخزَّن بمفتاح فريد ثابت (معرّف المستند نفسه)، دون تخمين اختصار إنجليزي.

- إزالة قيود `enum` الثابتة من مخططات Mongoose (`User.category/specializations`, `Course.category`, `AssignmentRequest.specialization`) — التحقق الفعلي انتقل لطبقة الخدمة (`isValidActiveKey`/`isKnownKey`، غير متزامن، عبر الكتالوج الحقيقي) في كل نقطة إنشاء/تعديل، فلا يمكن لأي نص عشوائي غير مُعتمد تجاوز الكتالوج.
- منع التكرار: تطبيع عربي (حذف التشكيل واختلافات الألف/الياء/التاء المربوطة عبر `utils/arabicNormalize.js` الموجودة مسبقًا) + تطبيع إنجليزي (حذف الفراغات الزائدة + توحيد الأحرف) — محاولة إنشاء منهج مكرر تُعيد المنهج الموجود فعليًا بدل تكراره.
- أرشفة (وليس حذف نهائي) — منهج مؤرشف لا يظهر للاختيار الجديد لكنه يبقى صالحًا لعرض السجلات التاريخية.
- صلاحيتان جديدتان `curricula.view`/`curricula.manage` (مُضافتان لصلاحيات الأدمن الافتراضية — تُطبَّق تلقائيًا على كل حساب أدمن قائم عبر ترحيل `rbacModulePermissionsBackfill` الموجود مسبقًا دون تعديل إضافي).
- مكوّن واجهة قابل لإعادة الاستخدام بالكامل `TeachingSubjectCombobox.jsx`: بحث فوري + إنشاء صريح فقط عند Enter/نقر (وليس مع كل ضغطة مفتاح) + تحديد فوري للمنهج الجديد + تنقّل كامل بلوحة المفاتيح + دعم RTL. مُطبَّق في: تخصصات المعلم (`SpecializationsMultiSelect.jsx`)، جدولة الطالب (`StudentScheduleSection.jsx`، وبالتالي معالج الإعداد وإضافة طالب وتعديل/إعادة إرسال الإسناد كلها تلقائيًا)، ونموذج تصنيف المقرر التعليمي (`AdminCourseFormPage.jsx`).
- كل مكان كان يعرض تسمية المنهج (بطاقات الطلبات، لوحة المعلم، الملف الإداري للمعلم، قائمة المعلمين) يقرأ التسمية الآن من الكتالوج الحي (`subjectLabel()` + `useTeachingSubjects()`) بدل مصفوفة ثابتة — منهج جديد يظهر بالاسم الصحيح فورًا في كل مكان.
- رسالة الإسناد العربية: التسمية تُحل من الكتالوج قبل بناء الرسالة (`resolveLabel`) وتُمرَّر كقيمة جاهزة (`curriculumLabelOverride`) — دالة بناء الرسالة تبقى نقية/متزامنة كما كانت. النص المُرسَل يبقى محفوظًا كما هو (`sentMessage`) فلو أُعيدت تسمية منهج لاحقًا، الرسائل التاريخية المُرسَلة فعليًا لا تتأثر — هذا مضمون فعليًا بالمعمارية الموجودة أصلًا.
- شاشة إدارية جديدة "المناهج التعليمية" (تبويب داخل `الإعدادات`) لإنشاء/أرشفة/إعادة تفعيل منهج، مقيّدة بصلاحية `curricula.manage`.

**ج) أخطاء حقيقية أُخرى اكتُشفت وأُصلحت أثناء هذا التدقيق:**
- اختباران قديمان في `sessionIntelligence.test.js` كانا يفترضان قواعد عمل قديمة (تسامح تأخر 15 دقيقة، ومكافأة المعلم مشروطة بحضور الطالب) بينما الكود الفعلي والموثَّق يطبّق قاعدة مختلفة ومقصودة (تسامح 5 دقائق، والمعلم يُصرف له أجره بصرف النظر عن حضور الطالب) — الاختباران كانا قديمَين لم يُحدَّثا بعد قرار عمل سابق موثَّق (تدقيق راتب المعلم، 2026-07-13)؛ صُححا ليطابقا السلوك الفعلي المقصود. **نتيجة: 0 اختبارات فاشلة في كامل حزمة الخادم لأول مرة (كانت هناك دومًا فشلتان "قديمتان" موثقتان منذ 2026-07-16).**

**اختبارات جديدة:** `teachingSubject.service.test.js` (15 اختبارًا: الزرع التلقائي، التحقق النشط/المعروف، تحليل التسمية، منع التكرار العربي/الإنجليزي، الأرشفة، تعارض إعادة التسمية)، `teacherAssignment.controller.test.js` (3 اختبارات: عزل ملكية نقطة نهاية التوفر)، إضافات على `assignment.service.test.js` (3 اختبارات لإعادة التحقق من الموعد المقترح) و`assignmentMessage.test.js` (اختباران لتجاوز التسمية الديناميكية)، وملف عميل جديد `teacherProfile.test.js` (7 اختبارات لدالة `subjectLabel`).

**التحقق:** `cd server && npx jest` → **380/380 ناجح** (صفر فشل، بما فيها الاختباران المُصححان). `cd client && npx vitest run` → **55/55 ناجح**. `cd client && npx eslint src` → صفر أخطاء (فقط تحذيرات قديمة غير مرتبطة). `cd client && npm run build` → صفر أخطاء. `node -e "require('./server.js')"` → شجرة المسارات الكاملة تُحمَّل دون أخطاء.

**قيود معروفة:**
- ~~لم يُنفَّذ تحقق يدوي حي بمتصفح فعلي في هذه الجلسة~~ — **مُنجَز لاحقًا**: جولة "جاهزية التسليم للجزء الأول" (2026-08-28، أدناه) نفّذت تحققًا حيًا كاملاً بمتصفح Chromium فعلي (عبر Playwright) على الخادم/الواجهة/قاعدة البيانات الفعليين.
- ~~شارات تصنيف المقرر في صفحتي تصفح المقررات لا تزالان تستخدمان خريطة ألوان/أيقونات ثابتة~~ — **مُغلَق لاحقًا**: نفس جولة "جاهزية التسليم للجزء الأول" أضافت `utils/subjectBadge.js` (لوحة ألوان محدودة وحتمية لكل منهج ديناميكي) وربطته بالصفحتين.

---

### 8. جولة جاهزية التسليم للجزء الأول (2026-08-28، تالٍ)

تفصيل كامل في `PROJECT_STATUS.md` (قسم "Phase 1 Delivery Readiness Pass") و`PHASE_1_DELIVERY_READINESS_REPORT.md`. جولة تحقق حي فعلية — ليست مراجعة كودية فقط — باستخدام متصفح Chromium حقيقي (Playwright، بعد تعذّر اتصال إضافة Claude-in-Chrome حتى بعد إعادة تشغيل كاملة) على نسخة تطوير حقيقية (خادم + واجهة + MongoDB).

**أخطاء حقيقية اكتُشفت وأُصلحت (لم تكتشفها أي مراجعة كودية سابقة):**
1. مكوّن `Input.jsx` المشترك: وسم `<label>` لم يكن مرتبطًا بـ`htmlFor`/`id` بحقل الإدخال — عيب إتاحة حقيقي في كل نموذج مبني على هذا المكوّن. أُصلح عبر `useId()`.
2. حذف طالب محفوظ أثناء الإعداد التدريجي (`AdminTeacherOnboardingWizardPage.jsx`) كان يحذف الحساب فعليًا بنقرة واحدة دون أي تأكيد — أُضيف `window.confirm()`.
3. خطّاف `useAuth.js` كان يرسل طلب `/auth/refresh` مكررًا فعليًا مرتين عند كل تحميل للتطبيق (بسبب `StrictMode` ودون `AbortController`) — أُصلح، تأكد الانخفاض من طلبين إلى طلب واحد.

**سيناريوهات نُفّذت حيًا بنجاح ودون أي خطأ في الطرفية (Console) أو طلب فاشل:** الإعداد الكامل لمعلم (كلمة مرور إدارية، إنشاء منهج ديناميكي أثناء المعالج) → جدولة ذكية لثلاثة طلاب (تأكد استبعاد كل حجز سابق تلقائيًا) → الإنهاء وصفحة النجاح → "إضافة المزيد من الطلاب" → طلب إسناد طالب جديد → تغيير كلمة مرور المعلم الإجباري → قسم الطلبات المعلّقة في لوحة المعلم → رسالة الإسناد العربية الكاملة → نافذة "اقتراح موعد بديل" المعاد تصميمها (اختيار يوم بلوحة المفاتيح فقط، بلا قيمة افتراضية ثابتة) → قائمة متابعة الإدارة وإعادة الإرسال.

**التحقق الآلي بعد الإصلاحات:** الخادم 380/380 (بلا تغيير)، العميل 63/63 (+8 اختبارات جديدة)، ESLint صفر أخطاء، بناء إنتاجي صفر أخطاء.

---

## 19. قرارات مطلوبة قبل التنفيذ

1. ~~ما المنطقة الزمنية الرسمية للأكاديمية~~ — **محسوم منذ الجزء الأول**: إعداد قابل للتغيير، الافتراضي `Africa/Cairo`.
2. هل يحق للمعلم تعديل أوقات عمله، أم الإدارة فقط؟ — **لا يزال مفتوحًا**؛ التنفيذ الحالي (الجزء الأول والثاني) يقتصر على "المسؤولون المخوّلون" فقط.
3. ~~هل توجد فترة فاصلة مطلوبة بين الحصص~~ — **محسوم في الجزء الثاني**: حقل إداري اختياري `AcademySettings.lessonBufferMinutes`، الافتراضي صفر (بلا فترة فاصلة) حتى تُفعّله الإدارة.
4. ~~هل الطالب القديم يُسند دائمًا دون موافقة~~ — **محسوم حسب نص طلب التنفيذ**: نعم دائمًا مباشرة؛ لم يُبنَ خيار إداري لطلب موافقة معلم على طالب قديم (يمكن إضافته لاحقًا كتوسعة صغيرة إذا احتاجته الإدارة).
5. هل التقرير الشهري يُنشأ في آخر يوم من الشهر أم أول يوم من الشهر التالي؟ — لا يزال مفتوحًا (خارج نطاق هذا التنفيذ، يخص §16).
6. هل يمكن للإدارة تعديل رسالة الإسناد لكل طلب فقط، أم تعديل القالب الافتراضي أيضًا؟ — **جزئيًا محسوم**: التعديل لكل طلب على حدة منفذ (`editedMessage`)؛ تعديل القالب الافتراضي نفسه من الإعدادات لم يُبنَ.
7. عند طلب المعلم تعديل الموعد، هل يحجز الموعد المقترح مؤقتًا حتى رد الإدارة؟ — **لا يزال مفتوحًا**؛ القرار المتخذ هندسيًا في هذا التنفيذ: لا يُحجز الموعد المقترح تلقائيًا، فقط الموعد الأصلي يبقى محجوزًا أثناء `pending_teacher_approval`.

---

## 20. تطبيق الموبايل

تطبيق الموبايل مقترح مستقل وغير داخل نطاق هذه المرحلة. عند اعتماده يتم إنشاء وثيقة نطاق منفصلة له.

---

## 21. سجل التغييرات

| التاريخ | التغيير | المصدر |
|---|---|---|
| 2026-08-24 | إنشاء الملف وتجميع جميع طلبات المرحلة الثانية السابقة | محادثات العميل والإدارة |
| 2026-08-24 | إضافة حساب المواعيد الشاغرة تلقائيًا حسب أوقات عمل المعلم والحصص المحجوزة | طلب جديد |
| 2026-08-24 | توضيح مسار الطالب القديم والجديد وقبول/رفض/إعادة إسناد الطالب | طلب جديد وصورة قالب الرسالة |
| 2026-08-25 | تنفيذ كامل لنطاق الإصدار التشغيلي الأول (§3): تصنيفات المعلمين والطلاب، أوقات العمل، نوع الطالب، معالج إنشاء المعلم مع طلابه، الرصيد الافتتاحي، الملف الإداري الأساسي، الصلاحيات وسجل العمليات. الأنظمة المتأثرة: `User`/`TeacherWorkingHours`/`OnboardingRequest`/`LessonTransaction`/`AcademySettings` (الخادم)، معالج ويزارد جديد وصفحة ملف معلم إدارية جديدة (الواجهة). التحقق: 243/245 اختبار ناجح، بناء الواجهة صفر أخطاء. التفاصيل الكاملة في قسم "متابعة تنفيذ الجزء الأول" أعلاه و`PROJECT_STATUS.md`/`ARCHITECTURE_PLAN.md`/`FEATURE_TRACKER.md` | تنفيذ الجزء الأول |
| 2026-08-25 | تنفيذ الجزء الثاني: خيارات بيانات دخول الطالب (تلقائي/يدوي + إلزام تغيير كلمة المرور)، محرك حساب المواعيد الشاغرة تلقائيًا (`services/availability.service.js`)، تفعيل الجدول الدوري الحقيقي من المعالج/إضافة طالب، ودورة طلب إسناد كاملة (نموذج `AssignmentRequest` + آلة حالات + قبول/رفض/تعديل موعد/تعديل وإعادة إرسال/إعادة إسناد/اعتماد فوري/إلغاء) مع رسالة إسناد عربية قابلة للتعديل وإشعارات لكل حدث. الأنظمة المتأثرة: `AssignmentRequest` (نموذج جديد)، `User`/`AcademySettings`/`Notification` (إضافات)، صلاحيات `assignments.view/manage/override` جديدة (الخادم)؛ معالج الويزارد وملف المعلم موسّعان بأقسام بيانات الدخول والجدول، وصفحتان جديدتان `طلبات الطلاب` (معلم) و`متابعة طلبات إسناد الطلاب` (إدارة) (الواجهة). التحقق: 320/322 اختبار ناجح، بناء الواجهة صفر أخطاء، **ومراجعة يدوية حية كاملة في متصفح فعلي ضد قاعدة بيانات ترتيلة الحقيقية** كشفت وأصلحت خطأين حقيقيين (رسالة الإسناد كانت تعرض `[object Object]`؛ الطلاب المجدولون بدون اشتراك كانوا غير ظاهرين في ملف المعلم). التفاصيل الكاملة في قسم "متابعة استكمال الجزء الأول وتنفيذ الجداول والإسناد" أعلاه و`PROJECT_STATUS.md`/`ARCHITECTURE_PLAN.md`/`FEATURE_TRACKER.md` | تنفيذ الجزء الثاني |
| 2026-08-25 | إعادة تصميم واجهة جدولة الطالب داخل المعالج بالكامل: استبدال عرض الأيام السبعة الدائم بمعالج مضغوط (تخصص→مدة→تكرار→أيام→مواعيد→تواريخ→مراجعة)، دعم تكرار "يوميًا" (كان مدعومًا في المحرك دون واجهة)، إصلاح تكرار "شهريًا" (كان سيتكرر أسبوعيًا خطأً)، منتقي مواعيد ذكي مقسّم صباحًا/ظهرًا/مساءً من محرك التوفر الحقيقي، جدولة مقترحة، تصحيح التحقق من التواريخ (خيار "يستمر حتى الإلغاء")، بطاقة ملخص أسبوعي ومعاينة شهرية قابلة للطي. مكوّنات جديدة: `WeekdayChipSelector.jsx`، `ScheduleSlotPicker.jsx`. أُضيف `vitest` كأول بنية اختبار للعميل (41 اختبار جديد لمنطق الجدولة النقي). **خطأ حقيقي اكتُشف وأُصلح أثناء التحقق اليدوي الحي:** توفر يوم كامل كان يُحسب كنافذة صفرية الطول محليًا لمعلم لم يُنشأ بعد، بسبب دالة تنسيق وقت جديدة لا تحافظ على حدّ نهاية اليوم `24:00`. التحقق: 325/327 اختبار خادم ناجح، 41/41 اختبار عميل ناجح، بناء الواجهة صفر أخطاء، وتحقق يدوي حي كامل (إنشاء وتفعيل جدول فعلي عبر البناء الجديد بنجاح). التفاصيل الكاملة في القسم الفرعي "4. إعادة تصميم واجهة جدولة الطالب المضغوطة" أعلاه | تحسين UX لاحق |
| 2026-08-26 | إصلاح معماري لمشكلة تكرار اقتراح المواعيد والحجز المزدوج عند إضافة عدة طلاب لنفس المعلم: تحويل المعالج من حفظ دفعة واحدة إلى حفظ تدريجي حقيقي — المعلم يُحفظ فوريًا قبل أي طالب (`onboardingStatus: 'draft'` حتى الإنهاء)، وكل طالب يُحفظ ويُحجز جدوله في استدعاء مستقل فور الضغط على "حفظ الطالب"، فيراه محرك التوفر فورًا للطالب التالي. نماذج جديدة: `OnboardingSession` (جلسة إعداد قابلة للاستئناف، منفصلة عن `OnboardingRequest` القديم الذي بقي كما هو)، `ScheduleReservationLock` (فهرس فريد `{teacherId, dayOfWeek, time}` لحماية تزامن ذرية حقيقية من القاعدة، مع رد 409 وبدائل حقيقية عند التعارض). دالة جديدة `suggestAlternativeSlots` في `availability.service.js`. المسار القديم دفعة واحدة بقي فعالًا دون تغيير. التحقق: 351/353 اختبار خادم ناجح (فشلان قديمان غير مرتبطين)، **وتحقق يدوي حي كامل بثلاثة طلاب فعليين** يطابق السيناريو المطلوب حرفيًا (استبعاد المواعيد المحجوزة تراكميًا، تحرر الموعد عند حذف الطالب، استئناف الجلسة بعد تحديث الصفحة دون تكرار، إنهاء ناجح وظهور المعلم في الدليل العام). التفاصيل الكاملة في القسم الفرعي "5. إصلاح معمارية الإعداد التدريجي متعدد الطلاب" أعلاه و`PROJECT_STATUS.md`/`ARCHITECTURE_PLAN.md`/`FEATURE_TRACKER.md` | إصلاح معماري لاحق |
| 2026-08-26 | تحسين تنقّل مركّز: إضافة زر أساسي "إضافة المزيد من الطلاب لهذا المعلم" في صفحة نجاح الويزارد، ينقل الأدمن مباشرة إلى `/admin/teachers/:id?action=add` (باستخدام معرّف المعلم الحقيقي) ويفتح تلقائيًا نافذة "إضافة طالب" الموجودة أصلًا، مع إعادة ترتيب أزرار الصفحة وإضافة بطاقة سياق مقفلة للمعلم داخل النافذة. صفر تغييرات خلفية — إعادة استخدام كامل لـ`POST /admin/teachers/:teacherId/students` والخدمات المرتبطة. التفاصيل الكاملة في القسم الفرعي "6. تحسين تنقّل مركّز" أعلاه | تحسين UX وملاحة منفصل |

